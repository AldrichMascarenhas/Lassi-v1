package com.example.credit_score.ui.data

import com.example.credit_score.model.CreditScoreHubData

val mockCreditScoreHubData = CreditScoreHubData(
    score = CREDIT_SCORE, maxScoreValue = CREDIT_SCORE_MAX_VALUE,
    title = "",
    subtitle = "",
    value = "$CREDIT_SCORE"
)